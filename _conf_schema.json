{
  "enabled": {
    "description": "Enable AstrContinuum. / 启用 AstrContinuum。",
    "type": "bool",
    "default": true
  },
  "model_context_limit": {
    "description": "Total model-context budget. v0.1.0 conservatively counts UTF-8 bytes, not provider tokenizer tokens. / 模型总上下文预算；v0.1.0 使用 UTF-8 字节保守计数，并非模型 tokenizer Token。",
    "type": "int",
    "default": 200000
  },
  "target_input_budget": {
    "description": "Preferred request-input budget in the same conservative unit. / 普通请求的目标输入预算，计量单位同上。",
    "type": "int",
    "default": 130000
  },
  "hard_input_ceiling": {
    "description": "Hard request-input ceiling in the same conservative unit. / 请求输入硬上限，计量单位同上。",
    "type": "int",
    "default": 150000
  }
}
