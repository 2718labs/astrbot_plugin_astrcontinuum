# V1-201 Immutable Domain Envelopes and Permanent Validators

Owner: domain-implementation-agent

Depends on: V1-101

## Goal

Create the canonical, immutable domain layer that V1-202 SQLite persistence can consume,
plus a deterministic pure-function permanent validator. Preserve the starter runtime as
a compatibility scaffold until its later migration card.

## Context

Read only:

- `../contracts/runtime-invariants.md`
- `../contracts/data-contracts.md`
- `../contracts/verification.md`
- `../../../superpowers/specs/2026-07-26-astrcontinuum-v1-design.md` sections 7, 9, 13,
  16, and 22
- `../../../../specs/event.schema.json`
- `../../../../specs/capsule.schema.json`
- `../../../../specs/snapshot.schema.json`
- `../../../../specs/compaction_job.schema.json`
- `../../../../astrcontinuum/models.py`
- the existing tests imported by `pytest`

Do not read sibling task cards. The four frozen JSON Schemas and shared contracts are
authoritative when prose and starter code differ.

## Write Scope

- `astrcontinuum/domain/__init__.py`
- `astrcontinuum/domain/_base.py`
- `astrcontinuum/domain/identity.py`
- `astrcontinuum/domain/events.py`
- `astrcontinuum/domain/capsules.py`
- `astrcontinuum/domain/snapshots.py`
- `astrcontinuum/domain/jobs.py`
- `astrcontinuum/domain/validation.py`
- `astrcontinuum/__init__.py`
- `tests/test_domain_envelopes.py`
- `tests/test_permanent_validator.py`

Strict MCP registration may checkpoint the existing parent directories `astrcontinuum`
and `tests` because the exact `domain` package and test files do not exist before the
write checkpoint. This card is the narrower authorizing scope.

## Steps

1. Run the unchanged full suite and record the baseline.
2. Create only `test_domain_envelopes.py`. Import the existing top-level package and use
   assertions for missing canonical exports so the RED is a behavioral failure rather
   than an import, fixture, syntax, or dependency error.
3. Confirm RED for SessionKey canonical identity/hash, Event mapping, frozen closed
   envelopes, and Snapshot/Job conditional rules; save the raw output.
4. Add the minimal `astrcontinuum.domain` implementation using Pydantic v2. Keep all
   existing starter model names and behavior available.
5. Run the focused domain-envelope suite green.
6. Create only `test_permanent_validator.py`, confirm the missing validator behavior RED,
   and save the raw output.
7. Implement a deterministic pure validator for SessionKey consistency, contiguous
   coverage, exact target equality, non-empty compiler output, valid active provenance,
   prior active semantic and anchor preservation, the four permanent quality gates, and
   the token ceiling.
8. Run both focused suites, the frozen Schema parser/contracts, all old tests, lint, and
   type checks.
9. Complete the strict output-index query and evidence gate without miss escape.

## Acceptance

Set all temporary, bytecode, and uv cache paths under
`<task-temp-root>`, then run:

```powershell
uv run --extra dev pytest tests/test_domain_envelopes.py -q
uv run --extra dev pytest tests/test_permanent_validator.py -q
uv run --extra dev python -c "import json, pathlib; [json.loads(p.read_text('utf-8')) for p in pathlib.Path('specs').glob('*.json')]"
uv run --extra dev pytest tests/contracts -q
uv run --extra dev pytest -q
uv run --extra dev ruff check .
uv run --extra dev mypy astrcontinuum
```

Pass conditions:

- SessionKey uses the seven-field canonical order, explicit nullable fields, UTF-8
  canonical JSON, and stable SHA-256 identity;
- EventEnvelope permits only the four Event/Role/Hook triples in the frozen Schema;
- every canonical envelope is frozen, closed to extra fields, and serializes all
  required nullable fields explicitly;
- Snapshot and Job conditional state rules match their frozen Schemas;
- permanent validation returns deterministic failure codes and cannot be disabled by
  the optional semantic-audit setting;
- existing in-memory models, tests, and public imports remain green;
- `main.py`, SQLite, compiler, auditor, wire Schemas, metadata, and release version are
  unchanged.

Strict completion additionally requires output index sync/query without miss escape and a
verification artifact bound to the output snapshot.

## Return

Return changed files, exact baseline/RED/GREEN outputs, output snapshot and trace ids,
verification artifact hash, contract decisions, and blockers.
