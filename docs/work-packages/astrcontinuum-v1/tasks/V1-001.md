# V1-001 Freeze v1 Architecture and Wave 1 Contracts

Owner: coordinator-doc-writer

Depends on: none

## Goal

Produce an evidence-backed v1 design, shared contracts, product brief, coordinator index,
and contract-only first implementation plan without editing production runtime code.

## Context

Read:

- `../../../../CODEX_MASTER_PROMPT.md`
- `../../../superpowers/specs/2026-07-26-astrcontinuum-v1-design.md`
- `../contracts/runtime-invariants.md`
- `../contracts/data-contracts.md`
- `../contracts/astrbot-composition.md`
- `../contracts/verification.md`

Confirmed facts:

- direct `ProviderRequest.contexts` replacement writes compacted history back to AstrBot;
- provider-only projection must restore in LIFO order around Sylanne;
- Sylanne already injects memory, so AstrContinuum initiates no Sylanne recall;
- the old Capsule Schema and database document cannot persist the required structured
  semantics;
- input index snapshot and checkpoint are recorded by the strict workflow.

## Write Scope

- `docs/superpowers/specs/2026-07-26-astrcontinuum-v1-design.md`
- `docs/superpowers/plans/2026-07-26-astrcontinuum-v1-wave1.md`
- `docs/work-packages/astrcontinuum-v1/product-brief.md`
- `docs/work-packages/astrcontinuum-v1/index.md`
- `docs/work-packages/astrcontinuum-v1/contracts/runtime-invariants.md`
- `docs/work-packages/astrcontinuum-v1/contracts/data-contracts.md`
- `docs/work-packages/astrcontinuum-v1/contracts/astrbot-composition.md`
- `docs/work-packages/astrcontinuum-v1/contracts/verification.md`
- `docs/work-packages/astrcontinuum-v1/tasks/V1-001.md`

The MCP checkpoint uses the existing parent `docs` because strict registration validates
paths before new files can exist. This card is the narrower authorizing scope.

## Steps

1. Bind a lease-scoped input query receipt to the indexed starter Snapshot.
2. Create a checkpoint before any file write.
3. Record standalone and Sylanne-enhanced boundaries without inventing an integration API.
4. Record the verified AstrBot Hook/save order and reversible identity contract.
5. Define methods and formulas for identity, Journal, read view, Capsule, retrieval,
   budget, jobs, validation, CAS, recovery, tool payloads, and observability.
6. Draw system, two-lane, projection sequence, and job-state Mermaid diagrams.
7. Define the contract-only Wave 1 RED/GREEN plan and exact first patch exclusions.
8. Validate work-package line budgets, required sections, and referenced paths.
9. Bind an output index, query it without miss escape, register hashed verification
   evidence, and complete under the same lease epoch.

## Acceptance

Run:

```powershell
$env:TEMP='<task-temp-root>\temp'
$env:TMP=$env:TEMP
$env:TMPDIR=$env:TEMP
uv run --extra dev python "<2718lab-devkit>\skills\work-methodology\scripts\validate_work_package.py" docs/work-packages/astrcontinuum-v1
```

Pass condition: exit code `0`, all required sections present, and every file respects its
line budget.

Then verify:

- every relative path mentioned by the package exists;
- all Mermaid fences are balanced and use supported diagram types;
- no placeholder marker exists;
- production code, Schemas, metadata, and version remain unchanged;
- output `project_index_sync(bind_as="output")` and lease-scoped query succeed;
- verification artifact is bound to that output snapshot before `workflow_complete`.

## Return

Return only changed paths, validation commands with actual output, input/output index and
trace ids, the architecture decisions, first patch boundary, risks, and blockers.
