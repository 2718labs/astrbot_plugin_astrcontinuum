# 安全策略

[English](./SECURITY.md) | 简体中文

> 本文件是 [English 原文](./SECURITY.md) 的简体中文译文，方便安全报告与部署人员阅读；如有差异，以英文原文为准。

AstrContinuum 会存储会话内容，并参与 Provider 请求装配。数据库披露、跨会话泄漏、投影持久化、身份冲突、不安全迁移、路径遍历、无界工具元数据和含有密钥的日志均应视为安全敏感问题。

## 受支持版本

| 版本 | 支持级别 |
| --- | --- |
| 仓库 `main` / `v0.3.0` | 接受安全修复和负责任的报告 |
| 较早的快照或无人维护的 fork | 不受支持 |

AstrBot 市场分发是独立的维护者流程；无论该分发流程如何，均接受针对仓库版本的安全报告。

## 报告漏洞

请使用仓库的[私密安全公告表单](https://github.com/2718labs/astrbot_plugin_astrcontinuum/security/advisories/new)。对于尚未修复的漏洞，请勿创建公开 Issue。

如可行，请包含：

- 受影响的提交/版本和 AstrBot 版本；
- 平台、适配器、Python 和操作系统的详细信息；
- 不包含真实会话内容或凭证的最小复现；
- 预期和实际观察到的安全边界；
- 对保密性、完整性、可用性或跨会话隔离的影响；
- 问题是否已被利用或已公开知晓。

请勿附上真实的数据库文件、访问令牌、Provider 密钥、用户标识符或私信。请使用合成夹具，并对路径和日志进行脱敏。

维护者将确认可用的报告，协调复现与修复，并通过私密公告协商披露时机。不保证精确的响应时限。

## 安全边界

- 本地 AstrBot 运营者控制文件系统和插件安装访问权限。
- 会话派生的 SQLite 值和规范 token 指标使用经过认证的 AES-256-GCM 信封；密钥文件和通过环境注入的密钥仍是运营者的机密。
- Journal 可以存储完整的用户和助手文本。
- 离线 tokenizer 资产按大小和 SHA-256 固定。运行时计数不得访问 tokenizer 网络或可变下载缓存。
- 外部记忆载荷不是可信的 Journal 来源。
- 私有 AstrBot 投影 API 会进行能力探测；不可用时以 fail-open 方式降级。
- 日志只包含经过脱敏的代码和有界元数据；排除模型身份、tokenizer 资产路径、消息文本、对象表示和密钥。
