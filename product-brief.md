# Capsule Recomposition Matrix 实验

## Goal

验证一种纯 Capsule 上下文缓存架构：旧 Capsule 与有界 delta 经过确定性矩阵重编排形成新 Capsule，随后释放旧代；运行时不再依赖 Summary。

## Scope

- 设计 Capsule Recomposition Matrix（CRM）、连续性内核、查询投影和安全释放协议。
- 设计多轮递归压缩、极限预算、效率和语义质量实验。
- Summary 只作为离线公平基线。
- 本轮在独立 D 盘实验包内实现并验证算法；不接本地模型、远程模型 API 或真实聊天。
- 本轮不修改或发布 `G:\AstrContinuum` 的生产代码、校验器、存储或运行时路径。

## Direction

- Capsule 是可释放缓存，不是权威历史存储。
- 当前状态由连续性内核 `K` 与弹性正文 `E` 组成。
- `K` 保留当前目标、焦点、未闭环事项、有效硬约束、最新有效决定和必要精确锚点；它每代重建、容量固定、不可完全驱逐。
- `E` 接受 CRM 的合并、缩写和释放。
- 错误或陈旧断言的代价远高于明确遗漏；缺乏支持时返回未知。
- 查询特定投影只能读取已经冻结的新 Capsule，不得读取旧代、delta、临时矩阵或 Summary。
- 后学习只能调整权重，不能绕过预算、溯源、冲突、连续性和风险硬约束。

## Risk Gate

- 任何有损候选均不得伪装通过现有永久校验，也不得进入生产发布路径。
- 新代未通过门禁并完成原子交换前，不得释放旧代。
- 初始预算低于连续性内核下限时拒绝启动；运行中的无效降预算不生效，保留上一有效预算下的当前内核并拒绝接纳新 delta。不得清空上下文或回退 Summary。
- 实验运行时与离线真值、查询及 Summary 基线必须隔离。

## Done

- 设计规格与实现计划已获用户复核；实现按逐任务 TDD、规格审查、质量审查推进。
- 后续实验按固定分母比较 CRM、旧 Capsule 与递归 Summary。
- 硬性目标为零连续性断裂、零陈旧事实误报、核心语义全保留和零预算超限。
